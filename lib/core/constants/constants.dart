class Constants {
  static const storagePath = '.storage';
  static const isAuthenticated = 'is_authentificated';
  static const authToken = 'auth_token';

  static const animationDuration = Duration(milliseconds: 300);
}